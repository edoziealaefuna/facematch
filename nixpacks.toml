[phases.setup]
nixPkgs = ["xorg.libX11", "xorg.libXext", "libGL", "cmake"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python app.py"

[build]
environment = { LD_LIBRARY_PATH = "/nix/store/*xorg*/lib" }